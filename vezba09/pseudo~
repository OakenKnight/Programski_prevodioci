//posle svake naredbe s ili u u zavisnosit od signed i unsigned
priprema:
	mov $0,i
for:
	cmpb nesto //gen_cmp koje radi sam relexp
	jge exit
	adds $1,i
	nesto //ovo se samo generise u statement
	jmp for
exit:
	nesto
